{
  "name": "firestore",
  "version": "1.0.0",
  "description": "Database package of poolscore",
  "private": true,
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "npx firebase-tools emulators:start --only firestore --import ./emulator_data"
  },
  "dev-dependencies": {
    "firebase": "^9.18.0",
    "firestore-tools": "^1.0.2"
  }
}
